// Data structure for translations
const translations = {
  en: {
    "nav.savories": "Savories",
    "nav.menu": "Menu",
    "nav.sweets": "SWEETS",
    "nav.arabicSweets": "Arabic Sweets",
    "nav.orientalSweets": "Oriental Sweets",
    "nav.special": "Special",
    "nav.cakes": "CAKES",
    "nav.specialCakes": "Special Cakes",
    "nav.customizedCakes": "Customized Cakes",
    "nav.cakesPlain": "CAKES",
    "nav.language": "English",
    "section.title": "Featured Sweets and Savories",
    "section.subtitle": "Discover Our",
    "modal.login": "Login to your account",
    "modal.signIn": "Sign In",
    "modal.email": "Email Address",
    "modal.pass": "Password",
    "toast.added": "Item added to cart",
    "btn.addCart": "Add to Cart",
    "btn.explore": "Explore Now",
    "btn.discover": "Discover Deals",
    "btn.orderCustom": "Order Your Cake",
    "hero.title": "CATHAREi",
    "hero.subtitle": "The Art of Traditional Arabic Sweets.",
    "hero.discover": "Indulge in the Artisanal Collection",
    "home.aboutTitle": "Doha's Finest Cake Shop & Bakery",
    "home.aboutText": "Welcome to CATHAREi – The Best Arabic Bakery & Pastry Shop. Craving authentic Middle Eastern desserts? At CATHAREi, we combine tradition with culinary artistry to bring you fresh Arabic pastries daily. From our signature hot cheese Kunafa delivery to our perfectly layered, authentic pistachio Baklava, every bite is crafted using premium ingredients. Whether you are searching for the best Arabic sweets near you or looking to order a luxury Arabic sweets gift box online, CATHAREi brings the rich heritage of Arabic sweets directly to your doorstep.",
    "home.promoTitle": "CATHAREi Bakery & Sweets",
    "home.promoText": "At CATHAREi, we believe that traditional Arabic desserts are more than just food—they are a celebration of culture, hospitality, and timeless heritage. As a premier Arabic bakery, we take pride in serving an exquisite selection of freshly baked delicacies, from savory staples like traditional Ka'ak bread and freshly baked pita bread to perfectly spiced Manakish Zaatar and cheese. For those with a sweet tooth, our pastry chefs specialize in authentic, heavy-hitting classics. We are renowned for our signature cheese Kunafa / Knafeh, standard-setting pistachio Baklava, melts-in-your-mouth Maamoul cookies with dates, and rich Basbousa. Whether you are looking for a casual weekend treat or searching for a Middle Eastern dessert shop open now, CATHAREi guarantees an unparalleled premium experience.",
    "home.exploreTitle": "Explore Our Delicious varieties!",
    "home.exploreText": "Crafting joy at midnight cravings on a feast is nothing that CATHAREi ensures in every bite.",
    "home.customTitle": "Custom cakes for Every Occasion",
    "home.customText": "Elevate Your Events with CATHAREi Luxury Dessert Catering. Make your next celebration unforgettable with the finest Middle Eastern dessert tables from CATHAREi. We specialize in premium catering for all occasions—offering beautifully curated Ramadan sweets boxes, bespoke Eid dessert catering, luxury Iftar dessert platters, and stunning corporate gifting options. From fresh, hot-delivered cheese Kunafa to elegantly packaged Arabic sweets gift boxes, our artisanal pastries add a touch of sophisticated tradition to weddings, family gatherings, and corporate events. Contact us today to customize your premium dessert box delivery.",
    "cat.savories": "Savouries",
    "cat.arabic": "Arabic Sweets",
    "cat.oriental": "Oriental Sweets",
    "cat.specialDes": "Special Desserts",
    "cat.specialCake": "Special Cakes",
    "cat.customCake": "Customised Cakes",
    "product.1": "Golden Baklava",
    "product.2": "Pistachio Kunafa",
    "product.3": "Rosewater Delight",
    "product.4": "Royal Honey Cake",
    "product.5": "Cardamom Maamoul",
    "product.6": "Dates & Walnuts",
    "product.7": "Saffron Pudding",
    "product.8": "Premium Basbousa",
    "product.9": "Cheese Fatayer",
    "product.10": "Spinach Triangle",
    "product.11": "Meat Samosa",
    "product.12": "Zaatar Mini Pizza",
    "faq.title": "Frequently Asked Questions",
    "faq.q1": "Do you offer in Doha delivery?",
    "faq.a1": "Yes, we offer delivery across Doha city.",
    "faq.q2": "Can I order cakes online?",
    "faq.a2": "Absolutely! You can customize and order directly through our website.",
    "faq.q3": "Do you make custom cakes?",
    "faq.a3": "Yes, we specialize in custom cakes for any occasion.",
    "footer.rights": "Copyright © 2026 CATHAREi. All rights reserved.",
    "footer.about": "The finest selection of traditional Arabic sweets, oriental pastries, and modern custom cakes in Doha.",
    "footer.links": "Links",
    "footer.help": "Help",
    "footer.follow": "Follow Us",
    "nav.home": "Home",
    "nav.contact": "Contact Us",
    "nav.faq": "FAQs",
    "nav.privacy": "Privacy Policy",
    "nav.terms": "Terms of Service",
    "nav.offers": "Offers",
    "nav.other": "Other",
    "nav.catering": "Catering & Occasions",
    "nav.blog": "Blog",
    "checkout.title": "Checkout",
    "checkout.billing": "Billing Details",
    "checkout.fullName": "Full Name",
    "checkout.phone": "Phone Number",
    "checkout.email": "Email Address",
    "checkout.address": "Delivery Address",
    "checkout.payment": "Payment Method",
    "checkout.cod": "Cash on Delivery",
    "checkout.card": "Card on Delivery",
    "checkout.summary": "Order Summary",
    "checkout.subtotal": "Subtotal",
    "checkout.total": "Total",
    "checkout.placeOrder": "Place Order Now",
    "checkout.deliveryTime": "Order will be only delivered in 1-2 Hrs",
    "v.title": "Verify Phone Number",
    "v.desc": "Please confirm that the phone number below is correct before we place your order.",
    "v.warning_en": "Your order will only be delivered after confirmation via a call to the customer.",
    "v.warning_ar": "لن يتم تسليم طلبك إلا بعد تأكيده من خلال مكالمة هاتفية مع العميل.",
    "v.confirm": "Confirm & Place Order",
    "v.cancel": "Cancel",
    "cart.title": "Your Cart",
    "cart.empty": "Your cart is empty",
    "cart.view": "View Cart",
    "cart.checkout": "Proceed to checkout",
    "menu.discover": "Discover our full collection of premium treats, from traditional Arabic sweets to modern designer cakes. Handcrafted daily in Doha.",
    "menu.selectSize": "Select Size",
    "partner.title": "Our Delivery Partners",
    "cake.scrollTitle": "The Finest Custom Cake",
    "cake.scrollText": "Handcrafted to perfection, this exclusive creation stands out as the absolute best custom cake in all of Doha. Delicate textures layered with rich flavors promise an unforgettable celebration.",
    "cart.product": "Product",
    "cart.qty": "Quantity",
    "cart.subtotal": "Subtotal",
    "cart.coupon": "Coupon code",
    "cart.apply": "Apply coupon",
    "cart.update": "Update cart",
    "cat.savoriesDesc": "Discover our handcrafted savory delights baked to perfection.",
    "cat.arabicSweetsDesc": "A rich tapestry of traditional flavors and premium pistachios.",
    "cat.cakesDesc": "Classic, fluffy, and freshly baked cakes for every occasion.",
    "cat.orientalSweetsDesc": "Delicate pastries inspired by the rich traditions of the Orient.",
    "cat.specialDesc": "Unique and exclusive desserts crafted for special moments.",
    "cat.specialCakesDesc": "Artisan cakes designed to make every celebration unforgettable.",
    "cat.customizedCakesDesc": "Bespoke cake designs tailored to your unique vision.",
    "status.pending": "Pending",
    "status.accepted": "Accepted",
    "status.rejected": "Rejected",
    "status.delivered": "Delivered",
    "status.sorryTitle": "Order Update",
    "status.sorryBody": "Thank you for choosing CATHAREi. We are truly sorry, but we are unable to accept your order at this time. Please contact us for more information.",
    "footer.stores": "Our Stores",
    "stores.aziziya": "Catharei Bakery - Doha (Salwa Road)",
    "stores.aziziyaAddress": "Building 384, Street 340 (Salwa Road), Zone 55, Al Waab / Al Aziziya, Doha, Qatar",
    "stores.wakrah": "Catharei Bakery - Al Wakrah",
    "stores.wakrahAddress": "Al Wakrah Main Street, Zone 90, Al Wakrah, Qatar",
    "stores.kharaitiyat": "Catharei Bakery - Al Kharaitiyat",
    "stores.kharaitiyatAddress": "Al Kharaitiyat Commercial Street, Zone 71, Al Kharaitiyat, Qatar",
    "brand.name": "CATHAREi",
    "brand.sub": "كاثاري",
    "home.title": "CATHAREI | Premium Arabic Sweets & Custom Cakes Qatar",
    "home.desc": "Discover luxury Arabic sweets, traditional pastries, and custom celebration cakes at CATHAREI. Visit our boutiques on Salwa Road, Al Wakrah & Al Kharaitiyat.",
    "customized_cakes.title": "Custom Celebration & Wedding Cakes Doha | CATHAREI",
    "customized_cakes.desc": "Order luxury custom cakes in Qatar. From premium wedding designs to eggless birthday cakes, CATHAREI crafts bespoke cakes for delivery across Doha.",
    "special_cakes.title": "Premium Specialty Cakes & Birthday Cakes | CATHAREI",
    "special_cakes.desc": "Explore CATHAREI’s signature specialty cakes. Handcrafted daily with premium ingredients, perfect for birthdays, anniversaries, and luxury gatherings.",
    "arabic_sweets.title": "Best Arabic Sweets, Kunafa & Baklava in Doha | CATHAREI",
    "arabic_sweets.desc": "Savor authentic Arabic sweets at CATHAREI. Fresh cheese Kunafa, premium Baklava trays, and luxury gifting boxes for Ramadan and Eid delivered across Qatar.",
    "oriental_sweets.title": "Artisanal Oriental Sweets & Pastries Doha | CATHAREI",
    "oriental_sweets.desc": "Indulge in premium oriental pastries, Maamoul, and gourmet pistachio desserts. Handcrafted traditional sweets beautifully packaged for luxury gifting.",
    "savories.title": "Fresh Savories, Fatayer & Catering Trays Doha | CATHAREI",
    "savories.desc": "Order fresh local savories, traditional mini fatayer, and premium gathering platters from CATHAREI. Perfect catering options for events and VIP gatherings."
  },
  ar: {
    "nav.savories": "موالح",
    "nav.menu": "القائمة",
    "nav.sweets": "حلويات",
    "nav.arabicSweets": "حلويات عربية",
    "nav.orientalSweets": "حلويات شرقية",
    "nav.special": "خاص",
    "nav.cakes": "كيك",
    "nav.specialCakes": "كيك خاص",
    "nav.customizedCakes": "كيك مخصص",
    "nav.cakesPlain": "كيك",
    "nav.language": "العربية",
    "section.title": "حلوياتنا وموالحنا المميزة",
    "section.subtitle": "اكتشف",
    "modal.login": "تسجيل الدخول",
    "modal.signIn": "دخول",
    "modal.email": "البريد الإلكتروني",
    "modal.pass": "كلمة المرور",
    "toast.added": "تمت الإضافة للسلة",
    "btn.addCart": "أضف للسلة",
    "btn.explore": "تصفح الآن",
    "btn.discover": "اكتشف العروض",
    "btn.orderCustom": "اطلب كيكتك",
    "hero.title": "كاثاري",
    "hero.subtitle": "نصنع لحظات من الدلال الخالص.",
    "hero.discover": "اكتشف المجموعة الحرفية",
    "home.aboutTitle": "أفضل متجر كيك ومخبز في الدوحة",
    "home.aboutText": "نقدم لكم أرقى تشكيلة من الحلويات العربية التقليدية والمعجنات الشرقية والكعك المخصص الحديث. مصنوعة بحب وأنقى المكونات.",
    "home.promoTitle": "حلويات ومعجنات كاثاري",
    "home.promoText": "سواء للاحتفال بالنجاح بفخر، دع مخبز الكعك الطازج في متجر الحلويات الخاص بنا في الدوحة يجلب إبداعاتنا المذهلة إليك مباشرة.",
    "home.exploreTitle": "استكشف أصنافنا اللذيذة!",
    "home.exploreText": "صنع الفرح في رغبات منتصف الليل أو في وليمة لا شيء سوى ما تضمنه كاثاري في كل قضمة.",
    "home.customTitle": "كعكات مخصصة لكل مناسبة",
    "home.customText": "اجعل الاحتفال مميزاً. أعطنا المخطط لما تحتاجه الكعكة لتعكسه ودعنا نجعل حلمك حقيقة.",
    "cat.savories": "موالح",
    "cat.arabic": "حلويات عربية",
    "cat.oriental": "حلويات شرقية",
    "cat.specialDes": "حلويات خاصة",
    "cat.specialCake": "كيك خاص",
    "cat.customCake": "كيك مخصص",
    "product.1": "بقلاوة ذهبية",
    "product.2": "كنافة فستق",
    "product.3": "حلاوة ماء الورد",
    "product.4": "كيكة العسل الملكية",
    "product.5": "معمول هيل",
    "product.6": "تمر وجوز",
    "product.7": "مهلبية زعفران",
    "product.8": "بسبوسة فاخرة",
    "product.9": "فطائر جبن",
    "product.10": "مثلثات سبانخ",
    "product.11": "سمبوسة لحم",
    "product.12": "ميني بيتزا زعتر",
    "faq.title": "الأسئلة الشائعة",
    "faq.q1": "هل تقدمون خدمة التوصيل في الدوحة؟",
    "faq.a1": "نعم، نقدم خدمة التوصيل في جميع أنحاء مدينة الدوحة.",
    "faq.q2": "هل يمكنني طلب الكعك عبر الإنترنت؟",
    "faq.a2": "بالتأكيد! يمكنك تخصيص وطلب كعكتك مباشرة عبر موقعنا.",
    "faq.q3": "هل تصنعون كعكات مخصصة؟",
    "faq.a3": "نعم، نحن متخصصون في الكعك المخصص لأي مناسبة.",
    "footer.rights": "حقوق النشر © 2026 CATHAREi. جميع الحقوق محفوظة.",
    "footer.about": "أرقى تشكيلة من الحلويات العربية التقليدية والمعجنات الشرقية والكعك المخصص الحديث في الدوحة.",
    "footer.links": "روابط",
    "footer.help": "مساعدة",
    "footer.follow": "تابعنا",
    "nav.home": "الرئيسية",
    "nav.contact": "اتصل بنا",
    "nav.faq": "الأسئلة الشائعة",
    "nav.privacy": "سياسة الخصوصية",
    "nav.terms": "شروط الخدمة",
    "nav.offers": "العروض",
    "nav.other": "المزيد",
    "nav.catering": "التجهيزات والفعاليات",
    "nav.blog": "المدونة",
    "checkout.title": "إتمام الطلب",
    "checkout.billing": "تفاصيل الفاتورة",
    "checkout.fullName": "الاسم الكامل",
    "checkout.phone": "رقم الهاتف",
    "checkout.email": "البريد الإلكتروني",
    "checkout.address": "عنوان التوصيل",
    "checkout.payment": "طريقة الدفع",
    "checkout.cod": "نقداً عند الاستلام",
    "checkout.card": "بطاقة (عند الاستلام)",
    "checkout.summary": "ملخص الطلب",
    "checkout.subtotal": "المجموع الفرعي",
    "checkout.total": "الإجمالي",
    "checkout.placeOrder": "اطلب الآن",
    "checkout.deliveryTime": "سيتم تسليم الطلب خلال ساعة إلى ساعتين",
    "v.title": "تأكيد رقم الهاتف",
    "v.desc": "يرجى التأكد من أن رقم الهاتف أدناه صحيح قبل إتمام الطلب.",
    "v.warning_en": "Your order will only be delivered after confirmation via a call to the customer.",
    "v.warning_ar": "لن يتم تسليم طلبك إلا بعد تأكيده من خلال مكالمة هاتفية مع العميل.",
    "v.confirm": "تأكيد وإتمام الطلب",
    "v.cancel": "إلغاء",
    "cart.title": "سلتك",
    "cart.empty": "سلتك فارغة",
    "cart.view": "عرض السلة",
    "cart.checkout": "إتمام الطلب",
    "menu.discover": "اكتشف مجموعتنا الكاملة من الحلويات الفاخرة، من الحلويات العربية التقليدية إلى كعكات التصميم الحديثة. تصنع يدوياً يومياً في الدوحة.",
    "menu.selectSize": "اختر الحجم",
    "partner.title": "شركاء التوصيل لدينا",
    "cake.scrollTitle": "أرقى كيكة مخصصة",
    "cake.scrollText": "صُنعت بإتقان لتكون الأفضل في الدوحة. طبقات رقيقة من النكهات الغنية تعدكم باحتفال لا يُنسى.",
    "cart.product": "المنتج",
    "cart.qty": "الكمية",
    "cart.subtotal": "المجموع الفرعي",
    "cart.coupon": "كود الخصم",
    "cart.apply": "تطبيق الكود",
    "cart.update": "تحديث السلة",
    "cat.savoriesDesc": "اكتشف موالحنا الحرفية المخبوزة بإتقان.",
    "cat.arabicSweetsDesc": "نسيج غني من النكهات التقليدية والفستق الفاخر.",
    "cat.cakesDesc": "كعكات كلاسيكية هشة ومخبوزة طازجة لكل مناسبة.",
    "cat.orientalSweetsDesc": "معجنات رقيقة مستوحاة من التقاليد الغنية للشرق.",
    "cat.specialDesc": "حلويات فريدة وحصرية مصممة للحظات الخاصة.",
    "cat.specialCakesDesc": "كعكات حرفية مصممة لتجعل كل احتفال لا يُنسى.",
    "cat.customizedCakesDesc": "تصاميم كيك مخصصة وفقاً لرؤيتك الفريدة.",
    "status.pending": "قيد الانتظار",
    "status.accepted": "تم القبول",
    "status.rejected": "تم الرفض",
    "status.delivered": "تم التوصيل",
    "status.sorryTitle": "تحديث الطلب",
    "status.sorryBody": "شكراً لاختياركم كاثاري. نعتذر جداً، ولكن لا يمكننا قبول طلبكم في الوقت الحالي. يرجى التواصل معنا للحصول على مزيد من المعلومات.",
    "footer.stores": "فروعنا",
    "stores.aziziya": "مخبز كاثاري - الدوحة (طريق سلوى)",
    "stores.aziziyaAddress": "مبنى 384، شارع 340 (طريق سلوى)، منطقة 55، الوعب / العزيزية، الدوحة، قطر",
    "stores.wakrah": "مخبز كاثاري - الوكرة",
    "stores.wakrahAddress": "شارع الوكرة العام، منطقة 90، الوكرة، قطر",
    "stores.kharaitiyat": "مخبز كاثاري - الخريطيات",
    "stores.kharaitiyatAddress": "شارع الخريطيات التجاري، منطقة 71، الخريطيات، قطر",
    "brand.name": "كاثاري",
    "brand.sub": "",
    "home.title": "كاثاري | حلويات عربية فاخرة وكيك مخصص قطر",
    "home.desc": "اكتشف أفخر الحلويات العربية، المعجنات الشرقية، وكيك المناسبات المخصص من كاثاري. زوروا فروعنا في طريق سلوى، الوكرة، والخريطيات. اطلب أونلاين الآن!",
    "customized_cakes.title": "كيك مخصص واحتفالات فاخرة في الدوحة | كاثاري",
    "customized_cakes.desc": "اطلب كيك مخصص فاخر لجميع مناسباتك في قطر. كعكات زفاف وأعياد ميلاد راقية ومصممة حسب الطلب مع خدمة التوصيل في الدوحة. صمم كعكتك الآن!",
    "special_cakes.title": "كعكات خاصة فاخرة وأعياد ميلاد | كاثاري",
    "special_cakes.desc": "تصفح تشكيلة كاثاري المميزة من الكعكات الخاصة. طازجة يومياً بأجود المكونات، مثالية لأعياد الميلاد، المناسبات، والتجمعات الفاخرة في قطر.",
    "arabic_sweets.title": "أفضل حلويات عربية، كنافة وبقلاوة في الدوحة | كاثاري",
    "arabic_sweets.desc": "تذوق أصالة الحلويات العربية من كاثاري. كنافة بالجبن طازجة، صواني بقلاوة فاخرة، وعلب هدايا راقية للأعياد والمناسبات مع التوصيل في قطر.",
    "oriental_sweets.title": "حلويات ومعجنات شرقية فاخرة في الدوحة | كاثاري",
    "oriental_sweets.desc": "استمتع بأفخر المعجنات الشرقية، المعمول التقليدي، وحلويات الفستق الفاخرة من كاثاري. حلويات تراثية مصنعة يدوياً ومثالية للهدايا الراقية في قطر.",
    "savories.title": "فطائر وموالح طازجة وصواني ضيافة الدوحة | كاثاري",
    "savories.desc": "اطلب موالح طازجة، فطائر صغيرة تقليدية، وأطباق تجمعات فاخرة من كاثاري. خيارات ضيافة مثالية للمناسبات والجمعات العائلية في قطر."
  }
};

const urlParams = new URLSearchParams(window.location.search);
const langParam = urlParams.get('lang');
if (langParam === 'en' || langParam === 'ar') {
  localStorage.setItem('catharei-lang', langParam);
}
let currentLang = localStorage.getItem('catharei-lang') || 'en';
let cartItems = JSON.parse(localStorage.getItem('catharei-cart-data') || '[]');
// Fallback array for empty cart scenario

document.addEventListener('DOMContentLoaded', async () => {
  initLanguage();
  updateCartBadgeUI();
  renderCartDropdown(); // Initially render the hover dropdown if it exists
  
  // Render based on what page we are on
  const grid = document.getElementById('product-grid');
  if(grid) {
    const category = grid.getAttribute('data-category');
    const isFeatured = grid.getAttribute('data-type') === 'featured';

    try {
      grid.innerHTML = Array(4).fill('<div class="product-card skeleton" style="height:350px; border-color:transparent;"></div>').join('');
      
      let url = '/api/products';
      if (category) url += `?category=${encodeURIComponent(category)}`;

      const response = await fetch(url);
      const products = await response.json();

      if (isFeatured) {
        renderProducts(products.filter(p => p.featured === 1).sort((a, b) => a.name.localeCompare(b.name)));
      } else {
        renderProducts(products.sort((a, b) => a.name.localeCompare(b.name)));
      }
    } catch (err) {
      console.error("Failed to load products:", err);
      grid.innerHTML = `<p style='text-align:center;width:100%;grid-column:1/-1;color:#999;'>Server offline. Please run <code>node server.js</code></p>`;
    }
  }

  setupEventListeners();
  applyTranslations();
  setupFAQ();
  syncCategoryCircleImages();
  initCakeAnimation();
  checkLoginStatus();


  // If on the cart.html page, render the cart list
  if(document.getElementById('cart-page-body')) {
    renderCartPage();
  }
});

function initLanguage() {
  const htmlTag = document.documentElement;
  htmlTag.setAttribute('dir', currentLang === 'ar' ? 'rtl' : 'ltr');
  htmlTag.setAttribute('lang', currentLang);
}

function updateCartBadgeUI() {
  const badge = document.getElementById('cart-badge');
  if(badge) {
    let count = 0;
    cartItems.forEach(item => count += item.qty);
    badge.textContent = count;
    badge.setAttribute('data-count', count);
  }
}

function setupEventListeners() {
  // Mobile Menu Toggle
  const menuToggle = document.getElementById('mobile-menu-toggle');
  const navList = document.getElementById('nav-list');
  if(menuToggle && navList) {
    menuToggle.addEventListener('click', () => {
      navList.classList.toggle('active');
    });
  }

  // Search Toggle
  const searchBtn = document.getElementById('search-btn');
  const searchContainer = document.getElementById('search-container');
  const searchInput = document.getElementById('search-input');
  if(searchBtn) {
    searchBtn.addEventListener('click', () => {
      searchContainer.classList.toggle('active');
      if(searchContainer.classList.contains('active')) {
        searchInput.focus();
      }
    });
  }

  // Cart Button Redirection
  const cartBtn = document.getElementById('cart-btn');
  if(cartBtn) {
    cartBtn.addEventListener('click', () => {
      let cartUrl = window.location.pathname.includes('/navigation/') ? 'cart.html' : 'navigation/cart.html';
      // Do not redirect if we are already on the cart page (pathname ends with cart.html)
      if(!window.location.pathname.endsWith('cart.html')) {
         window.location.href = cartUrl;
      }
    });
  }

  // Account Button Redirection
  const accountBtn = document.getElementById('account-btn');
  if(accountBtn) {
    accountBtn.addEventListener('click', () => {
      handleAccountClick();
    });
  }

  // Dropdowns for Mobile
  const dropdownParents = document.querySelectorAll('.has-dropdown');
  dropdownParents.forEach(parent => {
    parent.addEventListener('click', (e) => {
      if(window.innerWidth <= 768) {
        if(e.target.tagName === 'BUTTON' || e.target.parentElement.tagName === 'BUTTON') {
          // close others
          dropdownParents.forEach(other => {
            if(other !== parent) other.classList.remove('open');
          });
          parent.classList.toggle('open');
        }
      }
    });
  });
}

async function syncCategoryCircleImages() {
  const circles = document.querySelectorAll('.category-circle-item');
  if (circles.length === 0) return;

  try {
    const res = await fetch('/api/products');
    const products = await res.json();

    circles.forEach(circle => {
      const categoryId = circle.getAttribute('data-category');
      if (!categoryId) return;

      const img = circle.querySelector('.category-img');
      if (!img) return;

      // Find the first product in this category
      const product = products.find(p => p.category === categoryId);
      if (product && product.image) {
        let imgSrc = product.image;
        if (!imgSrc.startsWith('http') && !imgSrc.startsWith('/') && !imgSrc.startsWith('data:')) {
          imgSrc = '/' + imgSrc;
        }
        img.src = imgSrc;
      }
    });
  } catch (err) {
    console.error("Failed to sync category images:", err);
  }
}

function setupFAQ() {
  const faqItems = document.querySelectorAll('.faq-item');
  faqItems.forEach(item => {
    const question = item.querySelector('.faq-question');
    if(question) {
      question.addEventListener('click', () => {
        // close others (optional, but requested accordion style often implements this)
        faqItems.forEach(otherItem => {
          if(otherItem !== item) otherItem.classList.remove('active');
        });
        item.classList.toggle('active');
      });
    }
  });
}

function renderProducts(productsList) {
  const grid = document.getElementById('product-grid');
  if(!grid) return;
  grid.innerHTML = ''; // Clear

  if(productsList.length === 0) {
    grid.innerHTML = `<div style='grid-column:1/-1;text-align:center;padding:60px 20px;color:#666;'>
      <svg width="48" height="48" viewBox="0 0 24 24" fill="#333" style="margin-bottom:16px;display:block;margin-left:auto;margin-right:auto;"><path d="M11 9H9V2H7v7H5V2H3v7c0 2.12 1.66 3.84 3.75 3.97V22h2.5v-9.03C11.34 12.84 13 11.12 13 9V2h-2v7zm5-3v8h2.5v8H21V2c-2.76 0-5 2.24-5 4z"/></svg>
      <p style="font-family:'Playfair Display',serif;font-size:1.2rem;color:#D4AF37;margin-bottom:8px;">No products found</p>
    </div>`;
    return;
  }
  let schemaData = [];
  productsList.forEach(product => {
    const card = document.createElement('div');
    card.className = 'menu-item';
    
    // Dynamic naming handling for database
    let titleText = product.name;
    let titleI18nAttr = '';
    if (currentLang === 'ar' && product.name_ar) {
        titleText = product.name_ar;
    } else if (product.nameKey && translations[currentLang][product.nameKey]) {
        titleText = translations[currentLang][product.nameKey];
        titleI18nAttr = `data-i18n="${product.nameKey}"`;
    }

    let descriptionText = product.description || '';
    if (currentLang === 'ar' && product.description_ar) {
        descriptionText = product.description_ar;
    }

    // Resolve image path
    let imgSrc = product.image || '';
    if (imgSrc && !imgSrc.startsWith('http') && !imgSrc.startsWith('/') && !imgSrc.startsWith('data:')) {
      imgSrc = '/' + imgSrc;
    }

    // Premium pricing logic (handling variants)
    let priceHTML = product.price ? `QR ${parseFloat(product.price).toFixed(2)}` : '';
    if (product.variants && product.variants !== 'null') {
      try {
        const variants = typeof product.variants === 'string' ? JSON.parse(product.variants) : product.variants;
        if (variants) {
          const disabled = variants._disabled || [];
          const allSizes = ['small', 'medium', 'large'];
          
          const activeVariants = allSizes
            .filter(s => variants[s] !== undefined && !disabled.includes(s))
            .map(s => {
              const name = s.charAt(0).toUpperCase() + s.slice(1);
              return `${name}: ${variants[s]}`;
            });

          if (activeVariants.length > 0) {
            priceHTML = `<span style="font-size:0.8rem; font-weight:600;">${activeVariants.join(', ')}</span>`;
          }
        }
      } catch (e) { console.error("Error parsing variants", e); }
    }

    // Premium card structure matching menu.html
    card.innerHTML = `
      <img loading="lazy" src="${imgSrc || 'https://via.placeholder.com/400x300?text=' + product.name}" class="menu-item-img" alt="${titleText}">
      <div class="menu-item-info">
        <div>
          <div class="menu-item-name" ${titleI18nAttr}>${titleText}</div>
          <div class="menu-item-price">${priceHTML}</div>
          <div class="menu-item-desc">${descriptionText}</div>
        </div>
        <button class="btn btn-primary add-to-cart" style="width:100%; margin-top:15px; padding:10px; font-size:0.85rem;" 
          data-variants='${(typeof product.variants === 'string' ? product.variants : JSON.stringify(product.variants) || '').replace(/'/g, "&#39;")}'
          onclick="addToCart(event, '${product.id}', '${product.name.replace(/'/g, "\\'")}', ${product.price || 0}, '${(product.image || '').replace(/'/g, "\\'")}', this.dataset.variants)" 
          data-i18n="btn.addCart">Add to Cart</button>
      </div>
    `;
    grid.appendChild(card);

    // Build Product Schema
    schemaData.push({
      "@type": "Product",
      "name": titleText,
      "image": imgSrc ? ("https://catharei.com" + imgSrc) : "",
      "description": descriptionText || titleText,
      "offers": {
        "@type": "Offer",
        "url": "https://catharei.com/",
        "priceCurrency": "QAR",
        "price": product.price || "0.00",
        "availability": "https://schema.org/InStock"
      }
    });
  });

  // Inject Schema into DOM
  const oldSchema = document.getElementById('dynamic-product-schema');
  if (oldSchema) oldSchema.remove();
  const script = document.createElement('script');
  script.id = 'dynamic-product-schema';
  script.type = 'application/ld+json';
  script.text = JSON.stringify({
    "@context": "https://schema.org",
    "@graph": schemaData
  });
  document.head.appendChild(script);
}

let pendingVariantItem = null;

function addToCart(e, id, name, price, image, variantsData) {
  if(e) e.preventDefault();
  
  if (variantsData && variantsData !== 'null' && variantsData !== '') {
    try {
      const variants = JSON.parse(variantsData);
      openVariantModal(id, name, price, image, variants);
      return;
    } catch(err) { console.error("Error with variantsData", err); }
  }

  // Check if item exists in cart already
  let existingItem = cartItems.find(item => item.id === id);
  if(existingItem) {
    existingItem.qty += 1;
  } else {
    cartItems.push({ id, name, price, image, qty: 1 });
  }

  finalizeCartAddition();
}

function openVariantModal(id, name, price, image, variants) {
  pendingVariantItem = { id, name, price, image, variants };
  const modal = document.getElementById('variant-modal');
  const optionsWrap = document.getElementById('variant-options');
  if(!modal || !optionsWrap) return;

  let html = '';
  const disabled = variants._disabled || [];
  const availableSizes = Object.keys(variants).filter(key => key !== '_disabled' && !disabled.includes(key));
  
  availableSizes.forEach((size, index) => {
    const label = size.charAt(0).toUpperCase() + size.slice(1);
    html += `
      <label style="display: flex; align-items: center; gap: 12px; padding: 12px 15px; background: rgba(255,255,255,0.03); border: 1px solid #333; border-radius: 8px; cursor: pointer; transition: all 0.2s;">
        <input type="radio" name="product-size" value="${size}" style="accent-color: var(--color-accent); width: 18px; height: 18px;" ${index === 0 ? 'checked' : ''}>
        <div style="flex: 1;">
          <div style="font-weight: 600; color: #fff;">${label}</div>
          <div style="font-size: 0.85rem; color: var(--color-accent);">Qr ${variants[size].toFixed(2)}</div>
        </div>
      </label>
    `;
  });

  optionsWrap.innerHTML = html;
  modal.classList.add('active');
}

function closeVariantModal() {
  const modal = document.getElementById('variant-modal');
  if(modal) modal.classList.remove('active');
  pendingVariantItem = null;
}

function confirmVariantAddToCart() {
  if (!pendingVariantItem) return;
  
  const selectedRadio = document.querySelector('input[name="product-size"]:checked');
  if (!selectedRadio) return;

  const size = selectedRadio.value;
  const sizeLabel = size.charAt(0).toUpperCase() + size.slice(1);
  const price = pendingVariantItem.variants[size];
  const uniqueId = `${pendingVariantItem.id}_${size}`;
  const nameWithVariant = `${pendingVariantItem.name} (${sizeLabel})`;

  // Add to cart with unique ID and name
  let existingItem = cartItems.find(item => item.id === uniqueId);
  if(existingItem) {
    existingItem.qty += 1;
  } else {
    cartItems.push({ 
      id: uniqueId, 
      name: nameWithVariant, 
      price: price, 
      image: pendingVariantItem.image, 
      qty: 1 
    });
  }

  closeVariantModal();
  finalizeCartAddition();
}

function finalizeCartAddition() {
  // Save to persistent storage
  localStorage.setItem('catharei-cart-data', JSON.stringify(cartItems));
  
  // Update UI Elements
  updateCartBadgeUI();
  const badge = document.getElementById('cart-badge');
  if(badge) {
    badge.classList.remove('pop');
    void badge.offsetWidth; 
    badge.classList.add('pop');
  }

  showToast(translations[currentLang]["toast.added"] || "Item added to cart");
  renderCartDropdown(); // Refresh the DOM elements on the dropdown
}

function removeFromCart(id) {
  cartItems = cartItems.filter(item => item.id !== id);
  localStorage.setItem('catharei-cart-data', JSON.stringify(cartItems));
  updateCartBadgeUI();
  renderCartDropdown();
  if(document.getElementById('cart-page-body')) {
    renderCartPage();
  }
}

function updateCartQuantity(id, delta) {
  let existingItem = cartItems.find(item => item.id === id);
  if(existingItem) {
    existingItem.qty += delta;
    if(existingItem.qty <= 0) {
      removeFromCart(id);
      return;
    }
  }
  localStorage.setItem('catharei-cart-data', JSON.stringify(cartItems));
  updateCartBadgeUI();
  renderCartDropdown();
  if(document.getElementById('cart-page-body')) {
    renderCartPage();
  }
}

function renderCartDropdown() {
  const dropdown = document.getElementById('cart-dropdown');
  if(!dropdown) return;
  
  const emptyText = translations[currentLang]?.['cart.empty'] || "Your cart is empty";
  const totalText = translations[currentLang]?.['cart.total'] || "Total:";
  const viewCartText = currentLang === 'ar' ? 'عرض السلة' : 'View Cart';

  if(cartItems.length === 0) {
    dropdown.innerHTML = `<div style="padding:15px;text-align:center;color:#999;font-size:0.9rem;">${emptyText}</div>`;
    return;
  }
  
  let html = '<div class="cart-dropdown-list">';
  let total = 0;
  cartItems.forEach(item => {
    let sub = item.price * item.qty;
    total += sub;
    // Determine path depth dynamically based on where we are, but handle absolute URLs correctly
    let imgPath = item.image;
    if (window.location.pathname.includes('/navigation/') && !imgPath.startsWith('http') && !imgPath.startsWith('data:')) {
      imgPath = '../' + imgPath.replace('../', '');
    }
    
    html += `
      <div class="cart-dropdown-item">
        <img loading="lazy" src="${imgPath}" alt="${item.name}">
        <div class="cart-dropdown-info">
          <div class="cart-dropdown-title">${item.name}</div>
          <div class="cart-dropdown-price">${item.qty} x Qr ${item.price.toFixed(2)}</div>
        </div>
        <button class="cart-dropdown-remove" onclick="removeFromCart('${item.id}')">&times;</button>
      </div>
    `;
  });
  html += '</div>';
  
  // Calculate relative URL to cart page correctly depending on current directory
  let cartUrl = window.location.pathname.includes('/navigation/') ? 'cart.html' : 'navigation/cart.html';
  
  html += `
    <div class="cart-dropdown-footer">
      <div class="cart-dropdown-total">
        <span>${totalText}</span>
        <span>Qr ${total.toFixed(2)}</span>
      </div>
      <a href="${cartUrl}" class="btn btn-primary" style="display:block; text-align:center; padding:8px 0; margin-top:10px;">${viewCartText}</a>
    </div>
  `;
  dropdown.innerHTML = html;
}

function renderCartPage() {
  const container = document.getElementById('cart-page-body');
  const totalsBox = document.getElementById('cart-page-totals');
  const headerRow = document.getElementById('cart-table-header-row');
  const couponArea = document.getElementById('cart-coupon-area');
  
  if(!container || !totalsBox) return;

  if(cartItems.length === 0) {
    if (headerRow) headerRow.style.display = 'none';
    if (couponArea) couponArea.style.display = 'none';
    container.innerHTML = '<p style="padding: 20px 0; color: #999;">Your cart is currently empty. Explore our sweets!</p>';
    totalsBox.innerHTML = `
      <h3 style="margin-bottom:20px; font-size:1.2rem;">Cart totals</h3>
      <div style="display:flex; justify-content:space-between; margin-bottom:15px; border-bottom:1px solid #ddd; padding-bottom:15px;">
        <span>Subtotal</span>
        <span>Qr 0.00</span>
      </div>
      <div style="display:flex; justify-content:space-between; margin-bottom:25px; font-weight:bold;">
        <span>Total</span>
        <span>Qr 0.00</span>
      </div>
      <button class="btn btn-primary" style="width:100%; opacity:0.5; cursor:not-allowed;" disabled>Proceed to checkout</button>
    `;
    return;
  }

  if (headerRow) headerRow.style.display = 'grid';
  if (couponArea) couponArea.style.display = 'flex';

  let html = '';
  let subtotal = 0;
  cartItems.forEach(item => {
    subtotal += item.price * item.qty;
    html += `
      <div class="cart-table-row">
        <div class="cart-col-product">
          <img loading="lazy" src="${item.image}" alt="${item.name}">
          <div class="cart-item-meta">
            <h4>${item.name}</h4>
            <span class="price-strikethrough" style="display:none;"></span>
            <span class="price-actual">Qr ${item.price.toFixed(2)}</span>
          </div>
        </div>
        <div class="cart-col-qty">
          <div class="qty-adjuster">
             <button onclick="updateCartQuantity('${item.id}', -1)">-</button>
             <input type="text" value="${item.qty}" readonly>
             <button onclick="updateCartQuantity('${item.id}', 1)">+</button>
          </div>
        </div>
        <div class="cart-col-subtotal">
          <span class="subtotal-val">Qr ${(item.price * item.qty).toFixed(2)}</span>
        </div>
        <div class="cart-col-remove">
          <button onclick="removeFromCart('${item.id}')" aria-label="Remove item">
            <svg viewBox="0 0 24 24" fill="currentColor"><path d="M6 19c0 1.1.9 2 2 2h8c1.1 0 2-.9 2-2V7H6v12zM19 4h-3.5l-1-1h-5l-1 1H5v2h14V4z"/></svg>
          </button>
        </div>
      </div>
    `;
  });
  container.innerHTML = html;

  // Render totals
  totalsBox.innerHTML = `
    <h3 style="margin-bottom:20px; font-size:1.2rem; border-bottom:1px solid #e0e0e0; padding-bottom:15px;">Cart totals</h3>
    
    <div style="display:flex; justify-content:space-between; margin-bottom:15px; font-size:0.95rem;">
      <span>Subtotal</span>
      <span>Qr ${subtotal.toFixed(2)}</span>
    </div>


    <div style="display:flex; justify-content:space-between; margin-bottom:25px; font-weight:bold; font-size:1.1rem;">
      <span>Total</span>
      <div style="text-align:right;">
         <span>Qr ${subtotal.toFixed(2)} <small style="display:block;font-size:0.7rem;font-weight:normal;color:#999;">(includes Qr 0.00 VAT)</small></span>
      </div>
    </div>
    
    <button class="btn" style="width:100%; background-color:#D4AF37; color:white; border-radius:5px; padding:12px;" onclick="window.location.href='../checkout.html'">Proceed to checkout</button>
  `;
}

function showToast(message, type = 'success') {
  let container = document.getElementById('toast-container');
  if(!container) {
     container = document.createElement('div');
     container.id = 'toast-container';
     document.body.appendChild(container);
  }

  const icons = {
    success: '<svg class="toast-icon" viewBox="0 0 24 24"><path d="M12 2C6.48 2 2 6.48 2 12s4.48 10 10 10 10-4.48 10-10S17.52 2 12 2zm-2 15l-5-5 1.41-1.41L10 14.17l7.59-7.59L19 8l-9 9z"/></svg>',
    error: '<svg class="toast-icon" viewBox="0 0 24 24"><path d="M12 2C6.48 2 2 6.48 2 12s4.48 10 10 10 10-4.48 10-10S17.52 2 12 2zm1 15h-2v-2h2v2zm0-4h-2V7h2v6z"/></svg>',
    warning: '<svg class="toast-icon" viewBox="0 0 24 24"><path d="M12 2C6.48 2 2 6.48 2 12s4.48 10 10 10 10-4.48 10-10S17.52 2 12 2zm1 15h-2v-2h2v2zm0-4h-2V7h2v6z"/></svg>'
  };

  const toast = document.createElement('div');
  toast.className = `toast ${type}`;
  toast.innerHTML = `
    ${icons[type] || icons.success}
    <span>${message}</span>
  `;
  container.appendChild(toast);

  setTimeout(() => {
    toast.classList.add('fade-out');
    setTimeout(() => {
      toast.remove();
    }, 400);
  }, 4000);
}

window.setLanguage = function(langCode) {
  if(langCode === currentLang) return;
  const urlParams = new URLSearchParams(window.location.search);
  urlParams.set('lang', langCode);
  window.location.search = urlParams.toString();
};

function applyTranslations() {
  const dictionary = translations[currentLang];
  
  const elements = document.querySelectorAll('[data-i18n]');
  elements.forEach(el => {
    const key = el.getAttribute('data-i18n');
    if(dictionary[key] !== undefined) {
      el.textContent = dictionary[key];
    }
  });

  const inputs = document.querySelectorAll('[data-i18n-ph]');
  inputs.forEach(input => {
    const key = input.getAttribute('data-i18n-ph');
    if(dictionary[key]) {
      input.placeholder = dictionary[key];
    }
  });

  // Translate document title and meta description dynamically
  function getPageKey() {
    const path = window.location.pathname;
    if (path.endsWith('/') || path.endsWith('index.html') || path === '' || (!path.includes('.html') && !path.includes('/navigation/') && !path.includes('/blog/') && !path.includes('/locations/'))) return 'home';
    if (path.includes('customized_cakes.html')) return 'customized_cakes';
    if (path.includes('special_cakes.html')) return 'special_cakes';
    if (path.includes('Arabic_sweets.html')) return 'arabic_sweets';
    if (path.includes('oriental_sweets.html')) return 'oriental_sweets';
    if (path.includes('savories.html')) return 'savories';
    return '';
  }

  const pageKey = getPageKey();
  if (pageKey) {
    if (dictionary[`${pageKey}.title`]) {
      document.title = dictionary[`${pageKey}.title`];
    }
    if (dictionary[`${pageKey}.desc`]) {
      const metaDesc = document.querySelector('meta[name="description"]');
      if (metaDesc) {
        metaDesc.setAttribute('content', dictionary[`${pageKey}.desc`]);
      }
    }
  }
}


// --- Cake Scroll Animation ---
function initCakeAnimation() {
  const canvas = document.getElementById('cake-canvas');
  const section = document.getElementById('cake-scroll-section');
  const textOverlay = document.getElementById('cake-scroll-text');
  
  if (!canvas || !section) return;

  const context = canvas.getContext('2d');
  
  const frameCount = 240;
  // Compute image path depending on depth
  const isNav = window.location.pathname.includes('/navigation/');
  const prefix = isNav ? '../images/herosection_1/' : 'images/herosection_1/';
  
  const currentFrame = index => {
    // ezgif-frame-001.webp
    const paddedIndex = index.toString().padStart(3, '0');
    return `${prefix}ezgif-frame-${paddedIndex}.webp`;
  };

  const images = [];
  let loadedImages = 0;
  
  const preloadImages = () => {
    for (let i = 1; i <= frameCount; i++) {
      images[i] = new Image();
      images[i].onerror = () => { console.error("Failed to load frame", i, currentFrame(i)) };
      images[i].src = currentFrame(i);
      images[i].onload = () => {
        loadedImages++;
        if(i === 1) {
          updateImage(1);
        }
      };
    }
  };

  const updateImage = index => {
    if(!images[index]) return;
    
    // Fit image to canvas maintaining aspect ratio
    const img = images[index];
    if (!img.complete || img.naturalWidth === 0) return;

    const hRatio = canvas.width / img.width;
    const vRatio = canvas.height / img.height;
    const ratio = Math.min(hRatio, vRatio);
    const centerShift_x = (canvas.width - img.width * ratio) / 2;
    const centerShift_y = (canvas.height - img.height * ratio) / 2;  

    context.clearRect(0, 0, canvas.width, canvas.height);
    context.drawImage(img, 0, 0, img.width, img.height,
                      centerShift_x, centerShift_y, img.width * ratio, img.height * ratio);
  };

  const setCanvasSize = () => {
    canvas.width = window.innerWidth;
    canvas.height = window.innerHeight - 80; // minus header height
    // After resizing, update image dynamically if we have scroll data
    const scrollTop = html.scrollTop;
    let fallbackFrame = 1;
    if(section.offsetTop > 0) {
      const sectionScroll = scrollTop - section.offsetTop;
      let scrollFraction = sectionScroll / (section.offsetHeight - window.innerHeight);
      if (scrollFraction < 0) scrollFraction = 0;
      if (scrollFraction > 1) scrollFraction = 1;
      fallbackFrame = Math.min(frameCount, Math.floor(scrollFraction * frameCount) + 1);
    }
    // Only update if image is available, otherwise it will draw on load
    if (images[fallbackFrame] && images[fallbackFrame].complete) {
      updateImage(fallbackFrame);
    }
  };

  const html = document.documentElement;

  setCanvasSize();
  window.addEventListener('resize', setCanvasSize);

  preloadImages();

  window.addEventListener('scroll', () => {
    const scrollTop = html.scrollTop;
    
    // Check if section is in view
    const sectionTop = section.offsetTop;
    const sectionHeight = section.offsetHeight;
    const sectionBottom = sectionTop + sectionHeight;
    
    if (scrollTop + window.innerHeight < sectionTop || scrollTop > sectionBottom) {
      return; // out of view
    }

    const maxScrollObj = sectionHeight - window.innerHeight;
    const scrollFraction = (scrollTop - sectionTop) / maxScrollObj;
    
    let frameIndex = Math.floor(scrollFraction * frameCount) + 1;
    if (frameIndex < 1) frameIndex = 1;
    if (frameIndex > frameCount) frameIndex = frameCount;

    requestAnimationFrame(() => updateImage(frameIndex));
    
    // Evaluate overlay text fading. Fade in between 20% and 80% scroll
    if(textOverlay) {
      if(scrollFraction > 0.1 && scrollFraction < 0.9) {
        textOverlay.style.opacity = 1;
      } else {
        textOverlay.style.opacity = 0;
      }
    }
  });
}
let currentUser = null;

async function checkLoginStatus() {
  try {
    const res = await fetch('/api/auth/me');
    const data = await res.json();
    if (data.loggedIn) {
      currentUser = data.user;
    }
  } catch (err) {
    console.error("Error checking login status:", err);
  }
}

function handleAccountClick() {
  const isSubPage = window.location.pathname.includes('/navigation/');
  const root = isSubPage ? '../' : '';
  
  if (currentUser) {
    if (currentUser.role === 'admin') {
      window.location.href = root + 'admin.html';
    } else {
      window.location.href = root + 'account.html';
    }
  } else {
    window.location.href = root + 'login.html';
  }
}
